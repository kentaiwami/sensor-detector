apiVersion: batch/v1
kind: CronJob
metadata:
  name: record-presence
  namespace: sensor-detector
spec:
  schedule: "* * * * *"  # 毎分
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: record-presence
              image: __REGISTRY_HOST__:5000/sensor-detector:__IMAGE_TAG__
              command: ["./record-presence"]
              resources:
                requests:
                  cpu: 50m
                  memory: 32Mi
                limits:
                  cpu: 200m
                  memory: 64Mi
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: sensor-detector-secret
                      key: database-url
